# 捉影（截屏标注工具）— 需求规格 v1.1

> 状态：草案（待确认项见 §13）；工具级详细行为见 TOOLS-SPEC.md
> 日期：2026-07-23

## 1. 概述

**捉影**——一款 Windows 桌面截屏工具：全局热键呼出 → 框选区域 → 原位添加注释（画笔、形状、图章、箭头、文字、编号、马赛克、橡皮）→ 输出（剪贴板 / 文件 / 贴图到屏幕）。交互体验对标 Snipaste / 微信截图。

## 2. 技术选型与运行环境

- **框架**：Avalonia 11.x + .NET 10（LTS，支持至 2028-11）。
- **首发平台**：仅 Windows（Win10 1903+ / Win11）。
- **架构约束**：截屏捕获、全局热键、系统托盘、剪贴板等平台相关能力必须收敛到 `IPlatformService` 一类的抽象接口后面，第一版只实现 Windows 后端（P/Invoke），为未来跨平台留口。
- **DPI**：Per-Monitor DPI Aware v2；混合 DPI 多屏（如主屏 150% + 副屏 100%）为一级支持场景，不是边缘情况。

## 3. 应用生命周期

- **单实例**：重复启动时唤起已有实例（Mutex + 通知）。
- **驻留**：启动后最小化到系统托盘，无主窗口。托盘菜单：截屏、设置、退出。
- **全局热键**：默认 `Ctrl+1` 开始截屏，可在设置中修改；注册失败（冲突）时托盘气泡提示并允许改键。
- **开机自启**：设置项，默认关闭。

## 4. 截屏主流程（状态机）

```
空闲 → [热键/托盘点击] → 冻结屏幕+显示遮罩 → 选区调整 → 标注编辑 → 输出 → 空闲
                                  ↑______________[Esc 逐级退出]______________|
```

1. 触发瞬间**冻结**：立即捕获全部显示器画面作为底图（后续所有操作基于冻结帧，屏幕内容变化不影响）。
2. 每个显示器覆盖一个全屏遮罩窗口，显示冻结帧 + 半透明暗化。
3. **默认选区 = 触发时鼠标所在显示器的整个屏幕**（对应"默认当前屏幕全屏"需求）。
4. 用户可直接拖拽重新框选任意区域；选区确认前后均可调整（见 §5）。
5. 确认选区后（或直接在默认全屏选区上）进入标注编辑，工具栏出现。
6. 输出后所有遮罩关闭，回到空闲。

**取消逻辑**：Esc 逐级退出——文字编辑中 → 退出编辑；有选中元素 → 取消选中；否则 → 放弃本次截屏。右键空白处等效后退。

## 5. 选区交互

- 拖拽画矩形框选；框选过程中显示尺寸提示（`W × H` 像素，物理像素）。
- 选区确定后：8 个手柄调整大小，拖拽内部移动选区。
- **选区可随时调整**（通过"选择"工具）：注释元素锚定在冻结帧的屏幕坐标系上，选区仅是最终裁剪范围——调整选区不移动已有注释；落在选区外的注释在输出时被裁掉（编辑时以半透明提示其在选区外）。
- 鼠标位置显示放大镜（像素级对齐用），含当前点颜色值。**（P1，可放二期）**
- **多屏：选区支持跨越显示器**（已确认）。混合 DPI 下跨屏底图拼接与输出分辨率策略是本项目最高技术风险点：输出按各屏物理像素拼接，不同缩放比的屏幕以物理像素为准对齐（实现方案在架构设计中细化）。

## 6. 注释工具

工具栏：**选择（鼠标）、自由画笔、形状、图章（自定义图片）、箭头、文字、编号、马赛克、橡皮、撤销、重做、输出按钮组**。

各工具完整行为定义见 **TOOLS-SPEC.md**，此处仅列要点：

- **选择**：默认工具；调整截图选区 + 选中/编辑已有元素；绘制工具画完一笔自动切回。
- **自由画笔**：颜色、粗细、线型可选；完成后以包围矩形被选中编辑，路径本身不可修改。
- **形状**：矩形/椭圆，描边颜色、粗细、线型（实线/虚线/点虚线）、填充；样式可存为预设并可修改/删除预设。
- **图章**：用户导入 PNG/JPG/SVG 入库，像图章一样点击放置。
- **箭头**：拖拽绘制；端点可再编辑；头部随线宽等比。
- **文字**：多行；**仅颜色 + 透明度**属性，大小用缩放控制（无字号概念）；双击再编辑，编辑态维持旋转/缩放状态。
- **编号**：自动递增徽章，删除后自动重排。
- **马赛克**：块大小 2–10 物理像素可调；仅作用于底图；支持旋转。
- **橡皮**：注释组的裁剪遮罩（PS 蒙版式）——擦除区内所有注释（含之后新画的）不可见，不影响底图；固化后不可选中不可编辑，仅撤销可移除；点选被擦除的元素可使其移出遮罩组、整体恢复并永久豁免。

## 7. 通用编辑行为（矢量编辑器核心）

所有注释元素为矢量对象，最终输出时才与底图合成：

- **选中**：单击命中元素（考虑旋转后的命中测试）；选中显示包围框 + 缩放手柄 + 旋转手柄。
- **移动 / 缩放 / 旋转**：**所有元素**支持（橡皮固化后除外）；Shift 等比缩放 / 15° 旋转吸附；文字与编号仅 4 角手柄强制等比。
- **透明度**：所有元素 0–100% 可调，默认 100%。
- **文字特例**：双击进入编辑态（编辑框随旋转/缩放渲染）。
- **删除**：选中后 Delete 键。
- **层级**：按创建顺序叠放；右键菜单"上移/下移一层"。**（P1）**
- **撤销/重做**：`Ctrl+Z` / `Ctrl+Y`，命令模式实现，覆盖所有增/删/改/变换/橡皮/恢复/选区调整操作，第一版必须有。
- **样式修改**：选中元素后工具栏显示其属性面板，改动即时生效且可撤销。

## 8. 输出

三种方式（第一版全部实现）：

| 方式 | 触发 | 行为 |
|------|------|------|
| 复制到剪贴板 | 工具栏按钮 / `Ctrl+C` / 双击选区 | 合成 PNG 写入剪贴板，关闭遮罩 |
| 保存为文件 | 工具栏按钮 / `Ctrl+S` | 另存为对话框，默认 PNG；默认文件名 `Screenshot_yyyyMMdd_HHmmss.png` |
| 贴图到屏幕 | 工具栏按钮 / `F3` | 合成图变为无边框置顶窗口钉在原位；贴图窗口支持拖动、滚轮缩放、Esc/双击关闭、右键菜单（复制/保存/关闭）；允许同时存在多个贴图 |

- 合成输出按**物理像素**（100% 原始分辨率），不受 DPI 缩放损失。
- 输出后本次截屏会话结束（贴图窗口独立存活）。

## 9. 预设与配置持久化

- 位置：`%AppData%/Zhuoying/`（目录用拼音，避免路径含中文引发的兼容问题；界面显示名为"捉影"）。
- `settings.json`：热键、默认马赛克块大小、默认保存目录、开机自启、绘制后是否自动切回选择工具等。
- `presets.json` + `presets/` 目录：形状样式预设与图章图片（图片复制入库，不引用原路径）。

## 10. 设置界面

简单设置窗口（托盘菜单进入）：热键改键、默认保存路径、默认马赛克块大小、开机自启、连续绘制开关、各工具默认样式。

## 11. 非功能需求

- 热键按下到遮罩显示 ≤ 300ms（含捕获）。
- 标注编辑交互 60fps 不掉帧（拖拽/旋转/马赛克实时取样跟手）。
- 内存：空闲驻留 < 100MB。
- 混合 DPI 多屏（含跨屏选区）下选区与输出像素级准确（一级验收标准）。
- 异常不崩溃退出：捕获失败/热键冲突等给出托盘提示。

## 12. 明确不做（v1 范围外）

- 滚动截屏、延时截屏、录屏/GIF。
- OCR、翻译、二维码识别。
- 云上传/分享链接。
- 文字富文本（框内混合样式/粗体/斜体/列表等）——Avalonia 无内置富文本编辑器，成本 +100% 以上，长期不列入计划。
- 多语言（界面仅中文）。
- macOS / Linux 后端（仅保留接口）。

## 13. 待确认问题

1. ~~产品名称~~ 已定：**捉影**（配置目录 `Zhuoying`）。默认保存目录建议 `图片/捉影/`，待确认。
2. TOOLS-SPEC.md §11 中尚未确认的细节（画笔属性并集、橡皮不旋转、马赛克只作用底图、文字/编号强制等比、线宽随缩放、编号数字自动反差色），如有异议需在架构设计前定稿。橡皮遮罩语义已确认为纯组遮罩。
