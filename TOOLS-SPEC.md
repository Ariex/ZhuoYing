# 工具详细功能说明 v1.0

> 配套文档：REQUIREMENTS.md（需求主文档）。本文档定义工具栏中每个工具的完整行为。
> 日期：2026-07-23

## 0. 通用规则（适用于所有注释元素）

所有注释元素（画笔笔迹、形状、图章、箭头、文字、编号、马赛克）遵循统一模型：

**元素 = 几何数据 + 样式属性 + 变换（平移 / 缩放 / 旋转）+ 透明度**

例外：橡皮不是普通元素，见 §9。

### 0.1 变换（所有元素）
- **移动**：选择工具下拖拽元素本体。
- **缩放**：选中后 8 个手柄；按住 Shift 等比缩放；文字与编号仅提供 4 个角手柄（强制等比，见 §6/§7）。
- **旋转**：选中后包围框上方的旋转手柄，绕元素中心旋转；按住 Shift 吸附 15° 步进。
- 命中测试、手柄绘制均在元素旋转后的坐标系中正确工作。

### 0.2 透明度（所有元素）
- 范围 0–100%，默认 100%；选中元素后工具栏显示透明度滑块。
- 作用于整个元素（描边 + 填充 + 内容整体）。

### 0.3 选中与编辑
- 仅"选择"工具可选中元素（单选，v1 不做框选多选）。
- 选中后工具栏切换为该元素的属性面板（颜色 / 线宽 / 线型 / 透明度等，按元素类型显示可用项）；修改即时生效。
- Delete 删除选中元素。
- 层级：按创建顺序叠放；右键菜单"上移一层 / 下移一层"（P1）。

### 0.4 撤销 / 重做
以下操作全部纳入撤销栈：元素创建、删除、移动、缩放、旋转、任何属性修改（颜色 / 线宽 / 线型 / 透明度 / 马赛克块大小）、橡皮创建、被擦除元素的恢复、选区调整。

---

## 1. 选择（鼠标）

- 工具栏首位，进入标注编辑时默认激活；任何绘制工具完成一次绘制后**自动切回**选择工具（设置项可改为"连续绘制"）。
- **职责一：调整截图选区**——拖拽选区边框 8 手柄改变大小、拖拽选区边缘空白处移动选区；注释元素锚定屏幕坐标不随动，选区外注释半透明显示、输出时裁掉。
- **职责二：元素编辑**——单击选中元素；拖拽移动；手柄缩放 / 旋转；双击文字进入文字编辑；点选被橡皮部分擦除的元素使其恢复（见 §9）。
- 点击空白处取消选中；Esc：取消选中 → 再按放弃截屏。
- 重叠元素点击命中最上层者；再次点击同位置循环命中下一层（P1，v1 可仅命中最上层）。

## 2. 自由画笔（规格已按 0.8 实现定稿，取代早期草案）

- **创建**：按下左键拖动实时描线（2 物理像素步距抽稀采点，指针高频采样天然平滑），
  松开即完成一个笔迹元素；单点点击留圆点戳记；工具保持激活可连续绘制。
- **属性**：颜色（色板 + 取色器）、粗细（1–100 物理像素，默认 6）、**荧光**开关。
- **荧光笔**：笔迹与**底图（冻结帧）**做正片叠底（乘法混合）——黄荧光划过黑字
  字仍是黑的、白底变黄，即真实荧光笔效果。实现同区域模糊思路：采样底图、
  逐像素乘画笔色、按笔迹带状几何裁剪绘制（NonZero 填充，来回涂抹无孔洞、
  自身重叠不加深）；只与底图叠底，会压住其下的其他标注（同马赛克语义）。
- **完成后**：可选中（虚线包围盒）、整体拖拽移动、Delete 删除、改颜色/粗细/荧光、
  右键图层菜单；**笔迹路径本身不可编辑**，暂不提供缩放/旋转（同编号）。

## 3. 形状

- **内置形状**：矩形、椭圆。拖拽对角创建；按住 Shift 得到正方形 / 正圆。
- **属性**：
  - 描边颜色、描边粗细（1–20px）；
  - **线型**：实线、虚线、点虚线（dash-dot）；
  - 填充：无 / 纯色（填充色独立于描边色）；
  - 透明度。
- **样式预设**：可将当前形状样式（形状类型 + 上述属性组合）保存为预设，显示在工具二级栏；预设可**重命名、修改（覆盖保存）、删除**；持久化到 `presets.json`。
- 完成后可修改全部属性；移动 / 缩放 / 旋转均支持（缩放时描边粗细等比变化）。

## 4. 图章（自定义图片）（规格已按 0.9 实现定稿）

- **图库**：目录即库（`%AppData%/Zhuoying/stamps/`，SVG/PNG/JPG）；导入 = 文件复制入库
  （重名加序号）；内置 6 个 SVG 素材（✓ ✗ ⭐ ⚠ ❤ →）首次运行落盘，可删不复活；
  库内删除/重命名直接管理目录（不做界面）。
- **放置**：属性行素材选择器（缩略图横条 + "＋"导入）选中素材后，点击画布放置
  （默认高 160 物理像素、宽按素材比例、中心对齐点击点，按住可拖到位）；
  盖完自动回选择工具；同一素材可放多个实例。
- **属性**：透明度、**轮廓描边**（开关/颜色/粗细 1–40px）——沿素材不透明轮廓
  （alpha 距离变换，含镂空内缘），非矩形边框；描边与主体合成单张位图，
  半透明无叠加变深。
- 完成后支持移动 / 8 手柄缩放（允许非等比拉伸）/ 旋转；**透明区点击穿透**
  （命中按素材 alpha 采样）。
- SVG 按当前尺寸矢量光栅化（尺寸变化超 25% 重光栅化），任意缩放不糊；
  位图高质量重采样（Mitchell）。

## 5. 箭头

- **创建**：从起点拖拽到终点，松开完成；终点绘制实心三角箭头头部，头部大小随线宽等比。
- **属性**：颜色、线宽（1–20px）、线型（实线 / 虚线 / 点虚线，仅作用于线身，头部始终实心）、透明度。
- **完成后两种编辑方式并存**：
  - 选中后起点 / 终点各有一个端点手柄，拖动可直接改变箭头指向与长度；
  - 同时支持整体移动 / 包围框缩放 / 旋转。
- Shift 拖拽端点时吸附水平 / 垂直 / 45°。

## 6. 文字

- **创建**：点击画布在该处创建文本框并立即进入输入态；支持多行（Enter 换行）；点击框外或 Esc 结束编辑；结束时内容为空则自动删除该元素。
- **属性**：**仅颜色 + 透明度**。无字号设置——文字大小完全通过**缩放**控制（内部固定基准字号，缩放为矢量变换，任何大小都不模糊）。v1 明确不做：粗体 / 斜体 / 划线 / 字体切换 / 背景色 / 列表 / 框内混合样式。
- **再编辑**：选择工具下双击进入编辑态；**编辑态维持当前旋转角度和缩放比例显示**（编辑框随变换渲染），光标与选择操作在变换后的坐标系中正常工作。
- **缩放**：仅 4 个角手柄，强制等比（避免文字被拉扁）；文本框宽度随内容自适应，换行由用户手动 Enter 控制。
- 支持移动 / 旋转。

## 7. 编号（规格已按 0.6 实现定稿，取代早期草案）

- **创建**：点击画布放置圆形徽章 + 标号，自动递增；工具保持激活可连续放置。
- **形制**：实心圆（标号颜色按圆色灰度自动取黑/白反差色）/ 空心圆（圆环与标号同用所选颜色）。
- **标号类型**：`123`、`ABC`、`abc`、`i ii iii`、`I II III`、`一二三` 六种；
  **每种类型维护独立序列**（123 用到 10 后切一二三从"一"开始，切回 123 从 10 继续）。
- **下一个编号**：工具栏"№"控件显示，右侧上下小箭头（或滚轮）可调，默认从 1 开始。
- **属性**：大小（直径 10–200 物理像素，滑条弹层）、颜色（预设色板 + 取色器）。
- **大小恒定**：编号增大文字变多时徽章直径不变，内部文字自动缩小适配。
- **选中态**（不提供缩放/旋转手柄，仅虚线框 + 四角图标按钮，均带 tooltip）：
  - 左上 ▲▼：增/减该徽章编号值（不影响其他编号，允许出现重复值）；
  - 右上 红✕：删除该徽章（**不重排**其余编号）；
  - 右下 ↺：重置本序列——同标号类型的徽章按当前值排序后从最小值起重排连续
    （2,5,6,7,8 → 2,3,4,5,6），一个撤销单元，该类型的下一个编号顺延。
- 支持移动（本体拖拽）、Delete 删除、右键图层菜单。

## 8. 区域模糊（规格已按 0.7 实现定稿，取代早期"马赛克"草案）

像素化与模糊化两个子工具合并为一个工具栏按钮（同箭头/折线的组合方式）。

- **创建**：拖拽框定矩形区域，对区域内**底图（冻结帧）**做处理；无描边。
- **像素化**：块大小 1–50 物理像素，默认 10（子菜单滑条）。
- **模糊化**：模糊半径 1–50 物理像素，默认 10（子菜单滑条）；可分离箱式模糊双通近似高斯。
- **抗逆向**：像素化前按元素固定随机种子做**跨块**像素交换（块内乱序不改变块均值，
  必须跨块才能污染均值、抵抗 Depix 类按均值匹配还原的工具）；模糊化结果叠加 ±2 级
  确定性噪声（破坏反卷积去模糊）。种子随元素固定，重绘/移动不闪烁。
  注：这只抬高还原门槛，真正敏感内容仍应使用实心填充矩形覆盖。
- **永远在最底层**：渲染于所有其他标注之下、仅在底图之上（模型列表头部前缀组）；
  右键图层菜单只能在模糊组内调序，普通标注只能在其上方调序。
- **作用对象仅为底图**：不会模糊其他注释元素；移动 / 缩放实时重新取样底图。
- **旋转**：支持；块网格保持轴对齐，按旋转矩形裁剪边缘。
- 交互同矩形工具：8 缩放手柄 + 旋转手柄、本体拖拽移动、Delete 删除。

## 9. 橡皮（规格已按 0.10 实现定稿，取代早期"注释组遮罩"草案）

- **作用范围**：**只擦画笔/荧光笔笔迹**；形状、箭头、文字、编号、图章、
  区域模糊等一概不受影响，也不影响底图。
- **交互**：像画笔一样拖拽涂抹（圆形光标 = 橡皮大小，半透明灰 + 可见性边框），
  被擦区域实时消失；对该位置**所有**画笔笔迹一致生效（不分先画后画）。
- **属性**：只有大小（直径 1–100 物理像素，默认 24）。
- 橡皮痕迹本身不可选中、不可编辑；唯一恢复方式是撤销（Ctrl+Z）。
- **实现**：橡皮笔迹（带状几何并集）作为反向裁剪，渲染画笔元素时统一套上
  （`PushGeometryClip(全区 − 橡皮并集)`）——矢量语义，不逐笔迹处理、
  不破坏任何元素数据。

## 10. 工具栏总览

| 顺序 | 工具 | 快捷键（建议） |
|------|------|------|
| 1 | 选择 | V 或 Esc |
| 2 | 画笔 | P |
| 3 | 形状 | S |
| 4 | 图章 | I |
| 5 | 箭头 | A |
| 6 | 文字 | T |
| 7 | 编号 | N |
| 8 | 马赛克 | M |
| 9 | 橡皮 | E |
| — | 撤销 / 重做 | Ctrl+Z / Ctrl+Y |
| — | 输出组：复制 / 保存 / 贴图 | Ctrl+C / Ctrl+S / F3 |

## 11. 本文档中替你拍板的细节（不同意请指出）

1. **画笔完成后可改属性取并集**：颜色、粗细、线型、透明度均可改（你的描述中创建时提到"颜色、粗细"，完成后提到"颜色和线型"，前后不一致，按并集处理）；仅路径不可改。
2. **橡皮采用纯组遮罩语义**（已与你确认）：对组内所有元素（含之后新画的）生效；编辑不改变受遮罩状态；被恢复的元素永久豁免所有橡皮。
3. **橡皮不支持旋转**（你只提了创建中可缩放移动）。
4. **马赛克只作用于底图**，永不马赛克化其他注释；遮注释是橡皮的职责。
5. **文字 / 编号强制等比缩放**（只给 4 角手柄），避免拉扁变形。
6. **画笔 / 形状缩放时线宽等比变化**（另一种选择是线宽恒定，可改）。
7. **编号数字颜色自动反差**（不单独设置数字颜色）。
