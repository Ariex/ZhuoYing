# 工具详细功能说明 v1.0

> 配套文档：REQUIREMENTS.md（需求主文档）。本文档定义工具栏中每个工具的完整行为。
> 日期：2026-07-23

## 0. 通用规则（适用于所有注释元素）

所有注释元素（画笔笔迹、形状、图章、箭头、文字、编号、马赛克）遵循统一模型：

**元素 = 几何数据 + 样式属性 + 变换（平移 / 缩放 / 旋转）+ 透明度**

例外：橡皮不是普通元素，见 §9。

### 0.1 变换（所有元素）
- **移动**：选择工具下拖拽元素本体。
- **缩放**：选中后 8 个手柄；按住 Shift 等比缩放；文字与编号仅提供 4 个角手柄（强制等比，见 §6/§7）。
- **旋转**：选中后包围框上方的旋转手柄，绕元素中心旋转；按住 Shift 吸附 15° 步进。
- 命中测试、手柄绘制均在元素旋转后的坐标系中正确工作。

### 0.2 透明度（所有元素）
- 范围 0–100%，默认 100%；选中元素后工具栏显示透明度滑块。
- 作用于整个元素（描边 + 填充 + 内容整体）。

### 0.3 选中与编辑
- 仅"选择"工具可选中元素（单选，v1 不做框选多选）。
- 选中后工具栏切换为该元素的属性面板（颜色 / 线宽 / 线型 / 透明度等，按元素类型显示可用项）；修改即时生效。
- Delete 删除选中元素。
- 层级：按创建顺序叠放；右键菜单"上移一层 / 下移一层"（P1）。

### 0.4 撤销 / 重做
以下操作全部纳入撤销栈：元素创建、删除、移动、缩放、旋转、任何属性修改（颜色 / 线宽 / 线型 / 透明度 / 马赛克块大小）、橡皮创建、被擦除元素的恢复、选区调整。

---

## 1. 选择（鼠标）

- 工具栏首位，进入标注编辑时默认激活；任何绘制工具完成一次绘制后**自动切回**选择工具（设置项可改为"连续绘制"）。
- **职责一：调整截图选区**——拖拽选区边框 8 手柄改变大小、拖拽选区边缘空白处移动选区；注释元素锚定屏幕坐标不随动，选区外注释半透明显示、输出时裁掉。
- **职责二：元素编辑**——单击选中元素；拖拽移动；手柄缩放 / 旋转；双击文字进入文字编辑；点选被橡皮部分擦除的元素使其恢复（见 §9）。
- 点击空白处取消选中；Esc：取消选中 → 再按放弃截屏。
- 重叠元素点击命中最上层者；再次点击同位置循环命中下一层（P1，v1 可仅命中最上层）。

## 2. 自由画笔

- **创建**：按下左键拖动实时描线，松开即完成，生成一个笔迹元素。过短的笔迹（< 3px 位移）丢弃。
- 笔迹做轻度平滑（Catmull-Rom / 贝塞尔），避免锯齿折线感。
- **创建前属性**：颜色、粗细（1–20px）、线型（实线 / 虚线 / 点虚线）、透明度。
- **完成后**：以**包围矩形**作为选框被选中，支持移动 / 缩放 / 旋转；可修改颜色、粗细、线型、透明度；**笔迹路径本身不可编辑**（不能增删改锚点、不能重画局部）。
- 缩放时线宽随缩放比例等比变化（视觉上整体放大缩小）。
- 连续绘制模式下可连续画多笔，每笔是独立元素。

## 3. 形状

- **内置形状**：矩形、椭圆。拖拽对角创建；按住 Shift 得到正方形 / 正圆。
- **属性**：
  - 描边颜色、描边粗细（1–20px）；
  - **线型**：实线、虚线、点虚线（dash-dot）；
  - 填充：无 / 纯色（填充色独立于描边色）；
  - 透明度。
- **样式预设**：可将当前形状样式（形状类型 + 上述属性组合）保存为预设，显示在工具二级栏；预设可**重命名、修改（覆盖保存）、删除**；持久化到 `presets.json`。
- 完成后可修改全部属性；移动 / 缩放 / 旋转均支持（缩放时描边粗细等比变化）。

## 4. 图章（自定义图片）

- **图库**：用户从文件导入 PNG / JPG / SVG，文件复制到 `%AppData%/<App>/presets/` 入库（不引用原路径）；图库界面支持删除条目。
- **放置**：在图库中选中某图后，单击画布以默认大小（长边 128px，保持宽高比）放置一个实例；同一图片可放置多个实例。
- **属性**：透明度（无颜色 / 线型概念）。
- 完成后支持移动 / 缩放（Shift 等比；允许非等比拉伸）/ 旋转。
- SVG 矢量渲染，缩放不糊；位图放大超过原始分辨率时按平滑插值显示。

## 5. 箭头

- **创建**：从起点拖拽到终点，松开完成；终点绘制实心三角箭头头部，头部大小随线宽等比。
- **属性**：颜色、线宽（1–20px）、线型（实线 / 虚线 / 点虚线，仅作用于线身，头部始终实心）、透明度。
- **完成后两种编辑方式并存**：
  - 选中后起点 / 终点各有一个端点手柄，拖动可直接改变箭头指向与长度；
  - 同时支持整体移动 / 包围框缩放 / 旋转。
- Shift 拖拽端点时吸附水平 / 垂直 / 45°。

## 6. 文字

- **创建**：点击画布在该处创建文本框并立即进入输入态；支持多行（Enter 换行）；点击框外或 Esc 结束编辑；结束时内容为空则自动删除该元素。
- **属性**：**仅颜色 + 透明度**。无字号设置——文字大小完全通过**缩放**控制（内部固定基准字号，缩放为矢量变换，任何大小都不模糊）。v1 明确不做：粗体 / 斜体 / 划线 / 字体切换 / 背景色 / 列表 / 框内混合样式。
- **再编辑**：选择工具下双击进入编辑态；**编辑态维持当前旋转角度和缩放比例显示**（编辑框随变换渲染），光标与选择操作在变换后的坐标系中正常工作。
- **缩放**：仅 4 个角手柄，强制等比（避免文字被拉扁）；文本框宽度随内容自适应，换行由用户手动 Enter 控制。
- 支持移动 / 旋转。

## 7. 编号

- **创建**：点击画布放置圆形徽章 + 数字，数字自动递增（1, 2, 3 …）。
- **重排**：删除中间编号后，其后编号自动重排（1,2,4 → 1,2,3），重排作为同一撤销单元。
- **属性**：徽章底色（数字颜色自动取黑 / 白反差色）、透明度。
- 大小通过缩放控制（4 角手柄等比，同文字）；支持移动 / 旋转（数字随徽章整体旋转）。

## 8. 马赛克

- **创建**：拖拽框定矩形区域，对区域内**底图（冻结帧）**做马赛克化。
- **块大小**：2–10 物理像素，步进 1，默认 4；创建前在工具栏滑块选择，完成后选中可再改。
- **作用对象仅为底图**：马赛克不会模糊其他注释元素；任何注释元素（无论创建先后）渲染时始终位于清晰层。若需要遮挡注释请使用橡皮。
- 移动 / 缩放时马赛克实时重新取样其下方的底图内容。
- **旋转**：支持；旋转后对旋转矩形覆盖的底图区域取样马赛克化（边缘按元素形状裁剪）。
- **属性**：块大小、透明度（半透明马赛克 = 马赛克层与原底图混合）。

## 9. 橡皮（注释组遮罩）

橡皮不是普通元素，而是作用于整个注释组的**裁剪遮罩**（PS 组蒙版式）：

- **渲染模型**：最底层为冻结帧底图；所有注释元素（含马赛克）在一个"注释组"容器中；注释组的可见区域 = 全屏 − 所有橡皮矩形的并集（实现为容器 Clip 几何）；被"恢复"的元素移出该组，渲染在遮罩之上。
- **创建**：拖拽画出矩形框；**未固化前**可继续拖动移动、拖动手柄缩放（不支持旋转）；点击框外、按 Enter 或切换工具即固化。
- **效果**：遮罩对组内**所有**注释元素无差别生效——**包括橡皮创建之后新画的元素**；不影响底图。编辑已有元素（移动/变换/改样式）不改变其受遮罩状态：把元素挪进擦除区，重叠部分即被遮住。
- **所见即所得**：绘制新元素时的实时预览就在遮罩组内渲染，落入擦除区的部分在绘制过程中当场不可见，不会出现"松手后突然缺一块"。
- **固化后不可选中、不可编辑、不可移动、不可删除**；唯一移除方式是撤销（Ctrl+Z）。多个橡皮并存时遮罩为矩形并集。
- **恢复被擦元素**：选择工具下点选一个被橡皮（部分或全部）擦除的元素（点击其可见部分或原始轮廓内均可命中），该元素**移出遮罩组、整体恢复完全可见并置于最上层，从此豁免所有橡皮**（含未来新建的）；恢复操作可撤销。

## 10. 工具栏总览

| 顺序 | 工具 | 快捷键（建议） |
|------|------|------|
| 1 | 选择 | V 或 Esc |
| 2 | 画笔 | P |
| 3 | 形状 | S |
| 4 | 图章 | I |
| 5 | 箭头 | A |
| 6 | 文字 | T |
| 7 | 编号 | N |
| 8 | 马赛克 | M |
| 9 | 橡皮 | E |
| — | 撤销 / 重做 | Ctrl+Z / Ctrl+Y |
| — | 输出组：复制 / 保存 / 贴图 | Ctrl+C / Ctrl+S / F3 |

## 11. 本文档中替你拍板的细节（不同意请指出）

1. **画笔完成后可改属性取并集**：颜色、粗细、线型、透明度均可改（你的描述中创建时提到"颜色、粗细"，完成后提到"颜色和线型"，前后不一致，按并集处理）；仅路径不可改。
2. **橡皮采用纯组遮罩语义**（已与你确认）：对组内所有元素（含之后新画的）生效；编辑不改变受遮罩状态；被恢复的元素永久豁免所有橡皮。
3. **橡皮不支持旋转**（你只提了创建中可缩放移动）。
4. **马赛克只作用于底图**，永不马赛克化其他注释；遮注释是橡皮的职责。
5. **文字 / 编号强制等比缩放**（只给 4 角手柄），避免拉扁变形。
6. **画笔 / 形状缩放时线宽等比变化**（另一种选择是线宽恒定，可改）。
7. **编号数字颜色自动反差**（不单独设置数字颜色）。
