# 更新日志

版本规则见 README「版本号」节：`主.次` 为里程碑（手动），
文件版本第三、四段为构建时间戳（距 2026-01-01 天数 . 当日分钟数，自动）。

## 0.19 — 2026-09-06（阶段十八：Linux 移植）

- **Linux 支持**：X11 与 Wayland 两套后端，REQUIREMENTS v1 功能全部可用
  （抓屏 / 9 种标注 / 剪贴板 / 保存 / 钉屏 / GIF / MP4 / 全局热键 /
  开机自启 / Agent API / MCP）
- **平台边界收敛**：上层一律经 `Platform/PlatformServices` 取服务，
  `Platform/Windows` 与 `Platform/Linux` 互斥编译；新增 `IStartupManager` /
  `IWindowEffects` / `IFrameSource` / `IVideoEncoder` 四个接口。**Windows 行为不变**
- Wayland 抓屏走 xdg-desktop-portal ScreenCast + PipeWire，`persist_mode` 一次授权
  长期静默；全局热键走 portal GlobalShortcuts（要求 app id，须经 .desktop 启动）
- 新增 `build-linux.sh`（构建/运行/发布/AOT）与 `install-linux.sh`（安装桌面项）
- **Linux NativeAOT 可用**：`./build-linux.sh aot`，exe 32MB + 原生库共约 46MB，
  15 项回归全绿（含 SVG 光栅化与 UnmanagedCallersOnly 回调）
- **修正 GIF 编码器的 LZW 码宽 bug**（平台无关，Windows 同样受影响）：升位时机
  早了一个码，导致整帧码流比特错位。因帧间差分让小帧躲过升位点、且主流播放器
  对 LZW 错误容错，此前一直未被发现（TROUBLESHOOTING §16）
- 录制红框在不支持排除捕获的平台改用四条实心边条贴区域外沿，不再依赖窗口透明；
  控制条停不进区域外时隐藏，确保控件不入镜
- `--test-mp4` 采样点改为与显示器缩放无关（原按 200% 硬编码，无缩放屏上采到窗外）

## 0.18 — 2026-08-09（阶段十七：MCP 服务内置化）

- **MCP 服务改为托盘实例内置**（移除 `--mcp` 参数）：设置窗口勾选
  「启用 MCP 服务」即时启停、默认关闭——无需为 MCP 另起进程或重启程序；
  传输改为 MCP Streamable HTTP（仅绑定 127.0.0.1，端口默认 8990，
  settings.json `McpPort` 可改），Claude Code 用
  `{ "type": "http", "url": "http://127.0.0.1:8990/mcp" }` 一行接入
- HTTP 层为手写极简 HTTP/1.1（TcpListener）：绕开 http.sys URL ACL 权限、
  零依赖；校验 Origin 防 DNS rebinding；端口被占用有气泡提示
- 抓屏并发互斥：MCP 服务线程与截屏会话可能同时走 DDA，共享实例加锁
- 工具不变：take_screenshot / list_windows / get_monitors；
  `--api-*` 命令行保留（仍由「允许 Agent API」独立开关控制）

## 0.17 — 2026-08-09（阶段十六：MP4 录屏）

- **录制 MP4**（截屏会话中 F5 / 工具条 🎬 按钮）：与 GIF 录制同一套活屏
  流程（红框/控制条/24fps/含光标），输出 H.264/MP4——全彩、体积小一个
  数量级、时长无实用上限；适合长录制与高保真，GIF 适合随处内联播放
- 编码走 Windows 自带 Media Foundation（SinkWriter，优先硬件编码器，
  颜色转换自动插入；零新依赖，COM 互操作手写 vtable 函数指针，AOT 兼容）
- 码率按 0.1bpp×像素率自适应（1–25 Mbps）；H.264 要求偶数边长，区域自动
  向下取偶并同步红框；超编码器上限 4096×2304 时明确报错
- 时间戳用真实流逝时间，回放忠于实际；`--test-mp4` 自测含 SourceReader
  解码回读（帧数/尺寸/动画点色序/方向逐项验证）

## 0.16 — 2026-08-09（阶段十五：GIF 录屏）

- **录制 GIF**（截屏会话中 F4 / 工具条 ⏺ 按钮）：框选区域后关闭冻结帧、
  对选区**活屏录制**（24fps 采样、含光标）；区域红框（点击穿透）+
  控制条（时长/文件大小/完成/取消），两者 WDA_EXCLUDEFROMCAPTURE 不入镜；
  完成后存 GIF 到设定目录（同 Ctrl+S 目录），右下角气泡提示
- **流式 GIF89a 编码器**（手写零依赖）：逐帧写盘、内存与时长无关；
  帧间差分（脏矩形 + 未变化像素透明索引 + 处置法"保留"）、
  八叉树量化 ≤255 色局部色表、LZW 压缩、无限循环
- **时间轴忠于实际**：相同帧不重复写、时长自动并入（静止画面几乎不占体积），
  帧延迟按真实流逝时间写入（1/100 秒取整、误差滚动进位）——
  实测 3 秒录制解码总时长恰为 3000ms
- 自测参数 `--test-record "x,y,w,h[,fps[,毫秒]]"`（录屏核心 + 动画源）、
  `--test-record-ui "x,y,w,h[,毫秒]"`（完整 UI 流程）

## 0.15 — 2026-08-09（阶段十四：Agent API）

- **无头命令行**（供 AI Agent / 脚本获取屏幕信息，第二进程即用即退，
  与托盘实例互不干扰；坐标一律为虚拟屏幕物理像素）：
  - `--api-monitors`：显示器拓扑 JSON（物理边界/工作区/缩放比/主屏）
  - `--api-windows`：可见顶层窗口 JSON（标题 + 矩形，Z 序自顶向下，
    过滤规则与窗口吸附一致）
  - `--api-capture "x,y,w,h|full" [--out 路径]`：区域/全虚拟屏截图存 PNG
    （DDA→BitBlt 产线管线、携带来源 DPI；自带极简 PNG 编码器零框架依赖）
- **MCP 服务器**（`--mcp`，stdio JSON-RPC，手写实现零新依赖）：
  工具 take_screenshot（region/monitor/window_title 三选一定位，返回 PNG
  图像内容）/ list_windows / get_monitors；Claude Code 一行配置接入
- **安全边界**：只"看"不"动"（无输入注入）；默认关闭，
  设置窗口新增「允许 Agent API」开关（settings.json `AgentApiEnabled`）

## 0.14 — 2026-08-09（阶段十三：DDA 抓屏后端）

- **抓屏后端升级为 DXGI Desktop Duplication（BitBlt 自动回退）**：
  在合成/扫描输出层取帧，真·独占全屏游戏、MPO 硬件叠加平面（视频播放黑块）
  均能正确抓到；HDR 桌面按显示器 SDR 白电平做色调映射，不再洗白发灰；
  全虚拟屏（双 4K）抓取实测比 BitBlt 快约 3 倍（48ms vs 132ms），
  与 BitBlt 像素一致性达到屏幕动态本底（两路非同瞬抓取的差异 ≈ GDI 自身
  相隔 130ms 双抓的差异，即完全一致）
- 回退策略：D3D11 设备随托盘常驻预热；显示器每次抓屏现枚举（热插拔/改
  分辨率天然正确）；duplication 会话即建即弃（不长期禁用系统 MPO、不占
  其他抓屏软件的会话名额）；远程桌面、受保护内容、旋转屏、会话占满等
  任一环节失败仅该显示器区域回退 BitBlt，整体失败则全量回退，用户无感知
- AOT 兼容：DXGI/D3D11 全部为手写 COM vtable 函数指针调用，无内置 COM
  互操作依赖
- 自测参数 `--test-dda "x,y,w,h[|输出目录]"`：DDA 与纯 BitBlt 双路抓取，
  输出两张 PNG、逐像素 diff 与冷/热/GDI 耗时

## 0.13 — 2026-07-27（阶段十二：钉屏贴图窗口）

- **贴图到屏幕（F3 / 工具条图钉按钮）**：当前选区连同标注合成后钉成
  置顶小窗，初始落在选区原位、物理像素 1:1（像素级无损验证 0 差异）；
  按住拖动移动、滚轮缩放（10% 步进，100% 附近自动吸附回 1:1 并切换
  无损采样）、Ctrl+滚轮调透明度、双击/中键/Esc 关闭；
  右键菜单：复制到剪贴板（带来源 DPI）/ 另存为… / 关闭 / 关闭全部贴图；
  贴图独立于截屏会话，数量无上限（内存约束），跨屏拖动保持物理尺寸

## 0.12 — 2026-07-26（阶段十一：窗口吸附 / 右键重置 / 反色 / 持久化 / 生命周期）

- **开机自启**：设置窗口新增"开机自动启动"开关（HKCU Run 键）
- **通知气泡**：主屏右下角轻量提示（2.5s 自动消失、不抢焦点）——
  保存成功（含完整路径）/保存失败/热键被占用等场景不再静默
- **Alt + 点击下钻**：重叠元素中从当前选中层向下循环选择下一层
- "连续绘制"设置开关降级不做：各工具已按语义固化（画笔/折线/编号/图章/
  橡皮连续绘制，形状/箭头/文字/区域模糊画完回选择工具）

- **样式记忆落盘 presets.json**：各工具"上次使用的样式"（颜色/粗细/字号/
  荧光/描边/图章素材/橡皮大小等全部属性）持久化到
  `%AppData%\Zhuoying\presets.json`，重启后保持；每次截屏会话结束与退出时
  写盘（手工编辑该文件需保留全部字段）
- **"反色"颜色**：形状与画笔的色板末尾新增黑白对角特殊色块——标注颜色
  对底图逐像素取反（255−RGB），跨明暗背景每一段都自动反差、永远清晰；
  形状 = 描边环带/填充块几何裁剪，画笔 = 与荧光同管线（反色优先）；
  反色下线形/透明度不适用；`--test-shape` 第 11 参数、`--test-pen` 色值
  `INV` 可自测
- **窗口吸附**：截屏开始后（默认态）移动鼠标自动高亮其指向的窗口
  （绿色框 + 尺寸标签，DWM 扩展边界去阴影、按抓屏瞬间 Z 序、
  过滤最小化/隐身/点击穿透窗口），点击即把选区设为该窗口；
  按下拖动超阈值则照常转手动框选
- **右键重新开始捕捉**（微信截图式）：会话被改动过（选区/标注/历史）时
  右键 = 整体重置回初始状态——清空全部标注与撤销历史、回选择工具、
  选区恢复默认全屏、窗口吸附恢复、编号序列归 1；初始态再右键才退出截屏
  （原有级联保留：结束文字编辑 → 元素图层菜单 → 取消进行中绘制 → 重置 → 退出）

## 0.10 — 2026-07-26（阶段十：橡皮 + 笔刷光标 + 放大镜 + 保存）

- **保存（Ctrl+S）**：自动保存 PNG 到设定目录（设置窗口可改，留空 =
  "图片\捉影"，自动建目录），文件名 `捉影_年月日_时分秒.png`（重名加序号），
  保存后结束会话；PNG 写入 pHYs 块携带来源屏幕 DPI（192dpi 屏粘贴/打印不缩放）
- **另存为（Ctrl+Shift+S）**：系统保存对话框指定目录与文件名；取消则回到
  会话继续编辑
- 工具条复制按钮旁新增 保存 / 另存为 按钮（软盘 / 软盘+笔图标）
- 设置窗口新增"保存目录"配置（文本框 + 浏览）
- 新增自测参数 `--test-save x,y,w,h`

- 橡皮工具（E）：**只擦除画笔/荧光笔笔迹**，形状、箭头、文字、编号、图章、
  区域模糊一概不受影响；对先画后画的所有笔迹一致生效；只有"大小"一个设定
  （1–100px，跨会话记忆）；仅撤销（Ctrl+Z）可恢复被擦内容
  （实现：橡皮笔迹汇成反向裁剪几何，渲染画笔元素时统一套上——
  不逐笔迹处理、不破坏任何元素数据）
- 画笔/橡皮光标改为**实时圆形预览**：直径 = 工具粗细，画笔填当前颜色
  （荧光 = 半透明）、橡皮为半透明灰；外圈与区域模糊边界同款
  （1px 暗灰 + 白色渐弱发光），深浅背景都可见
- **跟随光标的放大镜**（PixPin 式）：像素网格放大 + 中心像素黑白双圈高亮 +
  坐标与颜色信息条，按 C 复制颜色值（#RRGGBB）；默认在光标右下方，
  空间不足自动翻到左/上侧；仅在选择工具且无选中元素时显示
  （纯选区语境，帮助像素级框选），无性能开销
- 新增自测参数 `--test-eraser`

## 0.9 — 2026-07-26（阶段九：图章工具）

- 图章工具（I）：素材图片贴入截图成为标注元素，点击放置（按住可拖到位）、
  移动 / 8 手柄缩放 / 旋转 / Delete / 右键图层菜单；透明区点击穿透
- 素材库：`%AppData%\Zhuoying\stamps\` 目录即库（SVG/PNG/JPG），
  内置 ✓ ✗ ⭐ ⚠ ❤ → 六个 SVG 首次运行落盘（删除不复活）；
  属性行素材选择器（缩略图横条 + "＋"导入文件）
- SVG 支持（新依赖 Svg.Skia 5.1，SkiaSharp 升至 3.119）：按当前尺寸
  矢量光栅化并缓存，尺寸变化超 25% 重光栅化——任意放大边缘锐利；
  位图素材 Mitchell 高质量重采样
- 轮廓描边（子菜单：开关/颜色/粗细）：对素材 alpha 做距离变换，
  沿不透明轮廓描边（含镂空内缘）而非矩形框；描边+主体+透明度合成
  单张位图，半透明无叠加变深
- AOT 回归通过（Svg.Skia 在 NativeAOT 下正常光栅化，仅无害裁剪警告）
- 新增自测参数 `--test-stamp`
- 描边按宽度向外扩画布：贴满画布的素材描边也完整显示、不被矩形裁掉
- 图章连续放置：选一次素材可连续点击盖多个（不再盖一个就回选择工具）
- **Ctrl + 角控制点 = 等比缩放**：形状/文字/图章/区域模糊全部盒状元素通用
  （以对角为锚，取两轴较大缩放因子，支持翻转）
- **样式跨会话记忆**：每个工具上一次修改的样式（颜色/粗细/字号/描边/素材
  选择等全部属性）在下一次截屏会话中保持（进程内记忆；旋转角新建仍归零）

## 0.8 — 2026-07-26（阶段八：画笔工具）

- 画笔工具（P）：拖拽自由描线（圆头圆拐角），单点点击留圆点，
  工具保持激活可连续绘制；可选中（虚线包围盒）、拖拽移动、删除、
  改属性、右键图层菜单
- 属性：颜色、粗细（1–100px 默认 6）、**荧光**开关
- 荧光笔 = 与底图正片叠底（乘法混合）：黄荧光划过黑字，字仍是黑的、
  白底变黄——Avalonia 无混合模式，实现走区域模糊同款思路
  （采样冻结帧 × 画笔色，按笔迹带状几何裁剪，NonZero 填充保证
  来回涂抹无孔洞、自身重叠不加深）
- 新增自测参数 `--test-pen`（竖线分隔可加多条笔迹）

## 0.7 — 2026-07-26（阶段七：区域模糊）

- 区域模糊工具：像素化（M）与模糊化（B）合并为一个工具栏按钮
  （悬浮弹出子选择，同箭头/折线的组合方式）
- 像素化：拖拽框定区域对底图打马赛克，块大小 1–50px（默认 10）
- 模糊化：模糊半径 1–50px（默认 10），双通箱式模糊近似高斯
- 抗逆向处理：像素化前按元素固定随机种子做跨块像素交换
  （污染块均值，抵抗 Depix 类还原工具）；模糊结果叠加肉眼不可辨的
  确定性噪声（破坏反卷积去模糊）；重绘/移动不闪烁
- 区域模糊永远在最底层（仅在底图之上）：后画的模糊也不会盖住任何标注，
  只对底图取样；右键图层菜单限制在模糊组内调序
- 无描边；其余交互同矩形工具：8 手柄缩放、旋转（块保持轴对齐、
  按旋转矩形裁剪）、移动、Delete、撤销/重做
- 模糊区域编辑期常显边界提示：1 物理像素暗灰描边 + 向外渐弱白色发光
  （深浅底图都可辨；复制输出仍无描边）
- 悬浮弹层堆叠方位彻底重做：方向由**工具条位置统一决定**——工具条每次
  停靠/拖动后检查其下方是否够放完整菜单链（三层 ≈ 720 DIP），够则所有
  层级向下堆叠，否则全部向上，各级方向一致（不再出现一级向下、二级向上
  互相覆盖的混乱）；堆叠以容器整体为基准：一级弹层弹在整条工具条的
  上/下方（不遮工具行），二级弹层弹在其父弹层的上/下方（逐层顺延）——
  修复全屏截图时子菜单弹出屏幕外不可见、及弹层覆盖工具条的问题
  （高度估算按弹层类型给最小值兜底：Button/ColorView 等模板控件
  未挂进视觉树时 Measure 结果接近 0，不能只信 DesiredSize）
- 弹层水平钳位：右缘超出屏幕时整体左移（工具条停靠屏幕右缘时必需）
- 弹层看门狗加一次宽限（600ms）：指针从按钮跨过工具条条带移向弹层时不误关
- 双工具按钮改 PS 风格：箭头/折线、像素化/模糊化的主按钮不再悬浮弹出选择
  小菜单（会与属性行同时出现、互相干扰）。主按钮图标为斜线分割的双图标
  （左上默认工具带蓝底 + 右下第二工具），点击激活组内最近使用的工具；
  属性行最左侧为组内选择器（当前工具图标 + 右下角小三角，与设置组之间
  以分隔线隔开）：点击后整行就地变为两个工具选择按钮（其余设置隐藏，
  当前工具高亮），选定后恢复对应工具的设置组
- 弹层水平规则：弹层比其容器（工具条/父弹层）宽时与容器右缘对齐向左扩展，
  否则与按钮左缘对齐向右扩展；屏幕边缘兜底钳位
- 修复工具条定位两处时序错误：启动首轮布局前窗口 Bounds 还是默认值，
  据其钳位会把工具条摆到半屏高度（改用虚拟屏幕尺寸/缩放直接换算）；
  属性行显隐后立刻 Measure 会被中间容器短路拿到旧尺寸导致底边溢出屏幕
  （重摆改为投递到布局完成后执行）
- 向上堆叠时工具条内部行序同步翻转：工具行在最下、属性行在其上方，
  与更上方的弹层方向一致（工具行→属性行→弹层全程同向）
- 区域模糊的"像素大小/模糊半径"滑条直接内联在属性行里（图标+滑条+数值），
  去掉一级弹层；拖拽仍合并为一条撤销，滚轮/单击立即入栈
- 文字工具新增整体透明度（0–100，属性行滑条）：同时作用于文字、文本框与
  描边（与文本框/描边各自的局部透明度相乘叠加）；编辑中实时预览
  （光标保持不透明便于编辑）；`--test-text` 追加透明度参数
- 自定义取色器改为**可拖拽的置顶取色浮窗**（PixPin 式）：真窗口承载 ColorView
  （旧 Popup 方案会把取色器左列裁掉一条且体积过大），宽度收窄到 250，
  顶栏"颜色"可拖动移动、右上 ✕/Esc/点击窗外关闭，选色实时预览画布，
  点击底部颜色预览条 = 确认并关闭；空间不足自动翻到按钮上方
- 工具条最左端新增拖拽手柄（两列圆点）：按住可把整个工具条拖到任意位置
  （钳在屏幕内）；拖过之后不再随选区自动停靠
- 新增自测参数 `--test-mosaic`

## 0.6 — 2026-07-26（阶段六：编号工具 + NativeAOT 兼容改造）

### 编号工具

- 编号工具（N）：点击画布放置圆形徽章，标号自动递增；工具保持激活可连续放置，
  按住可拖到准确位置再松开
- 形制两种：实心圆（标号颜色按圆色灰度自动取黑/白反差）、
  空心圆（圆环与标号同用所选颜色）
- 标号类型六种：123 / ABC / abc / i ii iii / I II III / 一二三；
  每种类型独立序列（切换类型各自续接，互不干扰）；
  字母为 Excel 列名式（Z 后 AA），罗马与汉字超出可表示范围自动退回阿拉伯数字
- 工具栏"№"控件显示下一个编号，上下小箭头 / 滚轮可调（默认从 1 开始）
- 徽章大小恒定：编号变长时内部文字自动缩小适配，直径不变；
  大小（直径 10–200px 滑条）与颜色（色板 + 取色器）可调
- 选中徽章后不提供缩放/旋转手柄，改为四角图标按钮（带 tooltip）：
  左上 ▲▼ 增减编号值（不影响其他编号，可出现重复值）、右上红✕删除（不重排）、
  右下 ↺ 重置本序列（同类型徽章从当前最小值起重排连续，一个撤销单元，
  下一个编号顺延）
- 新增自测参数 `--test-number`

### NativeAOT 兼容改造与发布

- 剪贴板写入由 OLE/COM 改为纯 Win32 API（`SetClipboardData` + HGLOBAL）：
  字节与格式完全不变（仅 CF_DIB + 正确 DPI 头），数据固化语义保持；
  移除 BuiltInComInteropSupport 与托管 IDataObject
- settings.json 序列化改为 System.Text.Json 源生成（文件格式不变：
  缩进、枚举字符串、未知字段忽略、缺失字段默认值）
- 全量回归通过：三场景复制像素级一致、进程退出后数据保持、
  剪贴板格式枚举干净、真实用户配置读取正常
- NativeAOT 发布打通（发布方法见 README）：exe 约 19MB；AOT 产物回归通过
  （剪贴板三场景、形状/箭头/文字渲染、ColorPicker 主题、源生成配置读取）
- 新增发布脚本 `aot-win.bat`（NativeAOT）与 `release-singlefile-win.bat`
  （自包含单文件 46MB），产物分别在 publish\aot 与 publish\singlefile

## 0.5 — 2026-07-25（阶段五：文字工具）

- 文字工具（T）：点击创建文本框并立即编辑；矩形内多行文本自动换行，
  同形状一样可移动/缩放/旋转
- 就地编辑用叠加真实文本框：中文输入法、光标、选择原生支持；
  旋转后的文字编辑时保持旋转角度显示；编辑框带边框与"输入文字…"占位符，
  编辑中工具栏样式修改（字号/颜色/加粗/对齐/内边距等）实时作用于编辑框
- 交互：点文本内部 = 进入编辑/移动光标，点边框环带 = 拖拽移动；
  空文本自动删除；点框外 / Esc / 右键结束编辑
- 样式：字体列表（滚动，无预览）、字号（默认 9–50，settings.json 可改范围）、
  颜色、加粗、斜体、水平/垂直对齐、外框子菜单（颜色/圆角/透明度/内边距）、
  描边子菜单（颜色/粗细/透明度）
- 元素右键菜单：上移一层 / 下移一层 / 移到顶层 / 移到底层（可撤销；
  已在顶/底时对应项禁用）
- 文字编辑中选中框与 8 控制点、旋转控制点保持显示（文字无圆角控制点）；
  "外框"子菜单更名"文本框"（开关文案"填充文本框"）；字号上限默认 100
- 编辑中描边实时预览：元素层同步渲染描边垫在编辑框之下（同一排版引擎逐字对位，
  IME 组词体验不受影响）；旋转控制点使用环形箭头光标
- 新增自测参数 `--test-text`
- 颜色选择器：接入官方 Avalonia.Controls.ColorPicker（ColorView）。
  三个工具属性行色板末尾及文字"文本框/描边"子菜单均有"自定义颜色"按钮
  （仅点击弹出，外点关闭；点击底部颜色预览条 = 确认并关闭），
  拖色谱画布实时变色、关闭合并为一条撤销；
  设置窗口每个预设色槽位也可用取色器选色（禁用 Alpha——透明度为独立属性）

## 0.4 — 2026-07-25（阶段四：箭头 / 折线工具）

- 箭头工具（A）：拖拽创建；默认起细尾粗 + 末端实心三角，画完自动回选择工具
- 折线工具（L）：逐次点击加点（实时预览）、双击结束、连续绘制；Esc/右键取消进行中折线
- 主工具栏"箭头/折线"合并按钮（悬浮弹出子工具选择）
- 端头样式起/末端独立可选（11 种）：实心/空心 × 三角/菱形/圆/方块、双线箭头、
  破甲箭头；端头随线宽等比，线身自动让位；下拉菜单所见即所得预览
- 起端/末端粗细独立（1–100px）：不等粗渲染为实心渐变带；等粗支持全部线形
- 弧线开关：Catmull-Rom 样条穿过全部控制点
- 线/箭头样式：颜色、线形、透明度（与形状同套交互）；箭头与折线独立记忆样式
- 编辑：控制点拖拽（十字光标）、线身拖拽整体移动、Delete、撤销/重做
- 标注模型泛化为多元素类型架构（形状/线共用命令栈与输入路由）
- 线形/端头选择器改为横向子工具条式面板（更紧凑，悬浮弹出）
- 修复半透明元素内部重叠变深（边框压填充、箭头压线身；TROUBLESHOOTING §11）：
  渐变箭头的三角/破甲头融合进渐变带单多边形；锐角折点斜切防自交
- 起端/末端粗细弹层内新增"应用到另一端"按钮；箭头上下文隐藏"弧线"开关
- 箭头工具默认末端粗细 40（起端 3）

## 0.3 — 2026-07-25（阶段三：标注编辑器核心 + 形状工具）

- 标注编辑器骨架：矢量元素模型 + 命令模式撤销/重做（Ctrl+Z / Ctrl+Y）
- 形状工具（快捷键 S）：矩形拖拽创建，圆角 0–100% 可调（100% = 椭圆）；
  选中后 8 个缩放手柄 + 四角 4 个圆角手柄（向中心拖增大圆角，越界钳位不回弹）
  + 旋转手柄（包围盒上方，Shift 吸附 15°）；形状内部可整体拖拽移动；Delete 删除
- 元素旋转：渲染/命中/手柄/输出合成全程一致；工具栏含旋转弹层（-180°..180°）
- 透明度 0–100%（整个元素，工具栏弹层调节）
- 形状样式：颜色（预设红黄绿蓝黑白，设置中可改、最多 10 个；选中色带彩虹渐变外框）、
  填充、线形（实线/虚线/点虚线/短横一点/短横两点，虚线段圆头、拐角圆连接，数据表驱动易扩充）、
  粗细（1–100px，紧凑滑条 + 滚轮微调，属性按钮上直接滚轮亦可）
- 工具栏改为两行：工具行 + 形状属性行（粗细/圆角为悬浮弹层，
  指针在按钮/弹层上或拖拽中不会关闭）
- 复制输出自动把标注按物理像素合成到截图上
- 设置窗口新增标注预设颜色编辑
- 新增自测参数 `--test-shape`
- 暂缓：聚光灯、"反色"颜色、元素旋转、透明度（后续阶段）

## 0.2 — 2026-07-25（阶段二：跨屏抓屏）

- 触发即冻结**整个虚拟屏幕**（一次抓取，各屏画面同一时刻），
  单一遮罩窗口覆盖所有显示器（PMv2 跨屏像素 1:1，不被系统缩放）
- 选区以虚拟屏幕物理像素为坐标系：可跨显示器拖拽新建、移动、8 手柄调整、
  选区外按下扩展（可点到另一块屏）
- 混合 DPI（主屏 200% + 副屏 150%）下输出按物理像素裁剪，跨屏选区像素级准确；
  输出 DPI 取选区覆盖面积最大的显示器
- 踩坑记录：曾用"每屏一个遮罩窗口"，窗口跨 DPI 移屏后 Avalonia 渲染与输入命中
  缩放分裂导致按钮点不中，最终改单窗口架构（docs/TROUBLESHOOTING.md §7–§10）
- 新增自测参数 `--test-copy` / `--test-select`（免键鼠注入的跨屏验证）

## 0.1 — 2026-07-25（阶段一：热键抓屏 + 单屏选区 + 复制）

- 全局热键截屏（默认 Ctrl+1，可在设置中改键；冲突时托盘提示并可改键绕开）
- 触发即冻结鼠标所在屏幕，全屏遮罩显示冻结帧，默认选区为该屏全屏
- 选区交互：拖拽画框、8 手柄调整（可越过对边翻转）、选区内拖拽移动、
  选区外按下即扩展到该点（松开保留 / 拖拽则画新矩形）、按方位联动光标
- 白色工具条（35×35 复制图标按钮），停靠选区右下；Enter / Ctrl+C / 双击等效复制
- 剪贴板输出：100% 物理分辨率 CF_DIB，携带来源屏幕 DPI；
  修复 Win11 画图粘贴被 DPI 减半的问题（根因："PNG" 自定义格式干扰，详见
  docs/TROUBLESHOOTING.md §1）
- 托盘驻留、单实例、设置窗口（改键，持久化 %AppData%/Zhuoying/settings.json）
- Per-Monitor DPI Aware v2；平台能力收敛至 Platform 接口层（Windows P/Invoke 实现）
- 版本号体系：Directory.Build.props 单一来源，构建自动打时间戳
  （文件版本 `主.次.天数.分钟`，产品版本 `主.次+本地时间±时区偏移`），UI 显示版本
